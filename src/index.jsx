import React from "react";
import ReactDOM from 'react-dom/client';

export const ShipMonitor = () => {
  return (
    <div>
      <h1>ANNIK-A Car-Go Monitor</h1>
    </div>
  );
};

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<ShipMonitor />);